var user = require(`readline-sync`)

var ca = Number(user.question("Qual o tamanho do CA do triangulo? "))
var co = Number(user.question("Qual o tamanho do CO do triangulo? "))
var hipotenusa = Number(user.question("Qual o tamanho da hipo do triangulo? "))

